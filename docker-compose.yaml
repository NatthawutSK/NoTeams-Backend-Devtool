version: "3.8"
services:
  backend:
    container_name: backend-server
    image: fixfireza/noteams-backend-devtool:2.0
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
